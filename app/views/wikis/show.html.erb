<h1><%= @wiki.title %></h1>

<%= link_to "Edit Wiki", edit_wiki_path, class: 'btn btn-info' %>

<div class="row">
  <div class="col-md-8">
    <p class="lead"><%= @wiki.body %></p>
  </div>

    <div class="col-md-8">
    <%= link_to "Delete Wiki", @wiki, method: :delete, class: 'btn btn-warning', data: {confirm: 'Are you sure you want to do that?'} %>
  </div>
   </div>
  </div>
